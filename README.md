Fetches the data from the api,
using DOM show dynamic values to the Interface.
core javascript does not suppport the .env so variable is placed in js file. copy it from .env and pasted there.
node is not used till now.